sudo: true
language: node_js
node_js:
  - 14.9.0 # use nodejs v10 LTS
cache: npm
install:
  - rm package-lock.json
  - npm install
branches:
  only:
    - hexo # build master branch only
script:
  - hexo clean
  - hexo generate 
deploy:
  provider: pages
  skip-cleanup: false
  github-token: $GH_TOKEN
  keep-history: false
  on:
    branch: hexo
  local-dir: public
